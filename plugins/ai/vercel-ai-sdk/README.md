# Vercel AI SDK

Planned plugin area for future Next.js AI features.

Likely packages:

```bash
npm install ai @ai-sdk/react
```

Use for:

- Streaming AI route handlers
- AI chat UI
- Generative product copy tools
- Future internal brand assistant

Not installed yet because the storefront does not currently have an AI user feature.
