# OpenAI

Planned plugin area for future OpenAI API integration.

Likely package:

```bash
npm install openai
```

Use for:

- Product copy drafts
- Brand voice experiments
- Future admin-only creative tools

Never commit API keys. Store them in `.env.local`.
